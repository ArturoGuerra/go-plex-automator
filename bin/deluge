#!/bin/sh

SCRIPTDIR=$(dirname $(readlink -f $0))

CONFIG="$SCRIPTDIR/config.json"
$SCRIPTDIR/goplex -mode="deluge" -deluge-torrentid=$1 --deluge-torrentname=$2 --deluge-torrentdir=$3 -configdir="$CONFIG"
